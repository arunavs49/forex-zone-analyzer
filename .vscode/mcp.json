{
  // MCP server configurations for VS Code and Copilot CLI
  //
  // Before connecting, set the token env var:
  //   export FOREX_MCP_TOKEN=$(./scripts/get-mcp-token.sh)
  //
  // VS Code: restart window after setting env var, or launch from terminal
  // Copilot CLI: set env var in same terminal before running `copilot`

  "servers": {
    "forex-zone-analyzer": {
       "type": "http",
       "url": "https://ca-forex-mcp.gentlegrass-9ef0e688.eastus2.azurecontainerapps.io/mcp",
       "headers": {
         "Authorization": "Bearer ${env:FOREX_MCP_TOKEN}"
       }
    }
  }
}
